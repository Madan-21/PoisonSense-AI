# Backend dependencies

# FastAPI & Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database (Postgres + pgvector)
sqlalchemy>=2.0.35
psycopg2-binary>=2.9.9    # Postgres driver
pgvector>=0.3.0           # pgvector SQLAlchemy support

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.2

# ML & AI  (NO local models — HF Inference API for embeddings, Groq for LLM)
# scikit-learn, pandas, numpy are NOT installed on Vercel (too large).
# The ML service (DistilBERT) gracefully falls back when unavailable.

# RAG Pipeline (API-based, no local models)
pdfplumber>=0.10.0
PyPDF2>=3.0.0
groq>=0.9.0               # Free LLM inference

# Validation & Utils
pydantic>=2.5.3
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
email-validator>=2.1.0

# HTTP Client (for external APIs)
httpx>=0.26.0
requests>=2.31.0

# Geolocation
geopy>=2.4.1

# Storage (Supabase REST API — no boto3 needed, uses httpx instead)
